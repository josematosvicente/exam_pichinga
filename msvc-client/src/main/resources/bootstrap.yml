spring:
  application:
    name: msvc-client
  profiles:
    active: dev
  cloud:
    config:
      name: msvc-client
      enabled: true
      profile: dev
      uri: http://localhost:8888
management:
  security:
    enabled: false